services:
  # --- Core Service ---
  ngelmak-core:
    build: .
    container_name: ngelmak-core
    ports:
      - "4043:4043"
    networks:
      - ngelmak-net

networks:
  ngelmak-net:
    external: true